services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: portal-frontend
    ports:
      - "3000:80"
    restart: unless-stopped
    networks:
      - portal-network

networks:
  portal-network:
    driver: bridge
